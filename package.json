{
  "name": "wu-personal-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:data": "node scripts/sync-data.mjs check",
    "fix:data": "node scripts/sync-data.mjs fix",
    "guard": "node scripts/pre-deploy-guard.mjs",
    "deploy:cf": "source .env && npx wrangler pages deploy dist --project-name wu-personal-site --branch main --commit-dirty=true",
    "deploy": "node scripts/sync-data.mjs deploy && npm run build && npm run guard && npm run deploy:cf"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "tweetnacl": "^1.0.3",
    "tweetnacl-sealedbox-js": "^1.2.0",
    "tweetnacl-util": "^0.15.1"
  }
}
